#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_jscodeshift@0.4.1@jscodeshift/bin/jscodeshift.sh" "$@"
  ret=$?
else 
  node  "$basedir/../../../_jscodeshift@0.4.1@jscodeshift/bin/jscodeshift.sh" "$@"
  ret=$?
fi
exit $ret
